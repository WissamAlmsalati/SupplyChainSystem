meta {
  name: Inventory List
  type: http
  seq: 12
}

get {
  url: {{baseUrl}}/inventory
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

tests {
  test("status is 200", function() {
    expect(res.getStatus()).to.equal(200);
  });

  test("returns inventory array", function() {
    const json = res.getBody();
    expect(json.data).to.be.an('array');
  });
}
